import 'package:flutter/material.dart';

class AuthModelSetup extends StatelessWidget {
  const AuthModelSetup({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
